package io.github.tanice.twItemManager.helper.mythicmobs;

import org.bukkit.entity.Player;
import org.jetbrains.annotations.NotNull;

public class MMHelper {
    // 只需要传入技能名称就可以调用对应的技能
    public boolean castSkill(@NotNull Player player, @NotNull String skillName, double damage, double k, double manaCost, double skillCooldown) {

        return true;
    }
}
